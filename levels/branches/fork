title = 创建平行提交 commit
cards = checkout commit-auto

[description]

你知道吗，创建平行时间线完全合法，也很安全。真的！

你能找出这个动物园里事情是从什么时候开始不对劲的吗？然后回到最后一个正常的提交 commit，创建一个所有人都开心的平行宇宙！

[cli]

蓝色小动物代表 Git 里的 “HEAD 指针”：它会告诉你当前位于哪个提交 commit。

这里有个回到上一个提交 commit 的小技巧：

    git checkout HEAD^

你也可以一次回退两个提交 commit，例如输入：

    git checkout HEAD~2

[setup]

mkdir cage
echo "Looks very hungry." > cage/lion

echo "A small child.
It really loves cats!" > child
git add .
git commit -m "The beginning"

echo "It's holding a lollipop." >> child
git commit -am "The child buys something"

mv child cage
git add .
git commit -m "The child climbs somewhere"

git rm cage/child
echo "Looks happy. :)" > cage/lion
git add .
git commit -m "Oh no"

echo "It's sleeping." > cage/lion
git add .
git commit -m "Nap time!"

git checkout --detach
git branch -d main

[win]

# 确保小孩是开心的。
git ls-tree --name-only -r HEAD | grep child

# 确保狮子有东西吃。
git show HEAD:cage/lion | grep -v "very hungry"

[congrats]

呼，干得好！这个结果看起来好多了。

你可以继续添加更多平行时间线，或者把它们延长。

准备好之后，下一个任务已经在等你了……
