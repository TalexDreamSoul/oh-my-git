title = 忽略文件

[description]

那只鸡一直跑来跑去，经常变化。我们不想把它放进提交 commit 里。

把它加入 `.gitignore` 文件，然后试着使用 `git add .`！

[setup]

touch .gitignore
echo important > important
git add important
git commit -m "Initial commit"

[actions]

echo "$RANDOM" > chicken
