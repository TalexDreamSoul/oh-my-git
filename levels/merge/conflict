title = 矛盾冲突
cards = checkout commit-auto merge reset-hard

[description]

有时候，不同时间线会互相矛盾。

例如这次，我们的一位客户想把这些时间线合并 merge 起来，但他们在两条时间线里吃了不同的早餐。

试着把它们合并起来！你会发现出现了冲突 conflict。时间机器会让你决定接下来怎么做：你可以编辑出问题的项目，它会显示发生冲突的部分。你可以保留两个版本中的任意一个，也可以把它们组合起来！删除 `>>>`、`<<<` 和 `===` 标记，然后创建一个新提交 commit 来完成这次合并 merge。

让最终完成的时间线成为 `main`。

[setup]

echo "Just woke up. Is hungry." > sam
git add .
git commit -m "The beginning"

git checkout -b pancakes
echo "Had blueberry pancakes with maple syrup for breakfast." > sam
git add .
git commit -m "Pancakes!"

echo "
Is at work." >> sam
git commit -am "Go to work"

git checkout -b muesli main
echo "Had muesli with oats and strawberries for breakfast." > sam
git add .
git commit -m "Muesli!"

echo "
Is at work." >> sam
git commit -am "Go to work"

git checkout main

[win]

# 在 'main' 分支 branch 里做出一份早餐折中方案。
git rev-parse main^ && test "$(git rev-parse main^1^^)" = "$(git rev-parse main^2^^)"

[congrats]

嗯，听起来是一顿不错的早餐！
