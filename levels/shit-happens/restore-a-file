title = 恢复被删除的文件
cards = checkout

[description]

糟糕，你删除了熬夜写出来的 `essay` 文件！

幸好 Git 可以帮忙！你可以用 `git checkout` 恢复这个文件！

[setup]

echo important > essay
git add .
git commit -m "Initial commit"
echo "important content" > essay
git commit -am "Improve essay"
rm essay

[win]

# 恢复 essay，让它包含 "important content"。
test "$(cat essay)" = "important content"
