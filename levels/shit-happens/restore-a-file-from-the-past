title = 从过去恢复文件
cards = checkout checkout-from commit

[description]

这里有个类似的问题：你很喜欢第一个提交 commit 里的那版 essay，想把它找回来！checkout 也可以从更早的提交中恢复内容，用法是：

    git checkout [commit] [file]

[setup]

echo "good version" > essay
git add .
git commit -m "Initial commit"
echo "bad version" > essay
git commit -am "\"Improve\" essay"

[win]

# 取回 essay 的第一个版本，并用它创建一个新的提交 commit。
test "$(git show main:essay)" = "good version"
