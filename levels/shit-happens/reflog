title = 回到你之前的位置
cards = checkout reflog

[description]

假设你正在查看过去的某个提交 commit，然后切回了 main 分支 branch。

但之后你彻底分心了。午休回来后，你想不起刚才位于哪个过去的提交 commit。怎么找回来？

有一个很方便的命令，可以显示 HEAD 过去指向过的所有位置：

    git reflog

[setup]

for i in {1..10}; do
    git commit --allow-empty -m $i
    git branch $i
done
git checkout 3
git checkout main

[win]

# 找出你之前去过哪里，然后回到那里！
test "$(git rev-parse HEAD)" = "$(git rev-parse 3)"
