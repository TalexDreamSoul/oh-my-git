title = 储藏 stash
cards = checkout commit-auto merge reset-hard

[description]

你会遇到这样的情况：正在项目里工作，但需要暂时把当前改动放到一边。为此，可以使用 stash 功能。使用
    git stash push 
把当前改动加入 stash 栈。

---
提示 1：先确认工作目录里有尚未提交的改动。
---
提示 2：`git stash list` 可以查看 stash 栈。
---
提示 3：完成后栈里至少应该有一条 stash 记录。

[setup]

echo "Apple Pie:" > recipe

git add .
git commit -m "creating a recipe"

echo "- 4 Apples" >> recipe

git add .
git commit -m "Adding ingredients"

echo "- 500g Flour" >> recipe

git checkout main

[win]

# 你储藏 stash 了当前改动吗？
test "$(git stash list | wc -l)" -ge 1

[actions]



[congrats]

储藏 stash 做得不错！
