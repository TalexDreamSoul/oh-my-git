title = 合并弹出的 stash
cards = checkout commit-auto merge reset-hard

[description]

当你想重新应用之前的改动，但已经继续修改了同一个文件，就可能遇到合并冲突 conflict。我们来练习这种情况。
用下面的命令从 stash 弹出改动：
    git stash pop
然后解决合并冲突 conflict。提交 commit 解决后的改动，并在之后清空 stash 栈。

---
提示 1：冲突文件里会出现需要你手动处理的标记。
---
提示 2：保留两个有用的配料行，然后删除冲突标记。
---
提示 3：最后别忘了提交 commit，并清空 stash 栈。

[setup]

echo "Apple Pie:" > recipe

git add .
git commit -m "creating a recipe"

echo "- 4 Apples" >> recipe

git add .
git commit -m "Adding ingredients"

echo "- 500g Flour" >> recipe

git stash push

echo "- Pinch of Salt" >> recipe

git checkout main
git add recipe

[win]

# 你解决冲突并提交 commit 了吗？
{ git show HEAD | grep "Flour"; } && { git show HEAD | grep "Salt"; }

# 你清空 stash 栈了吗？
test "$(git stash list | wc -l)" -eq 0

[actions]



[congrats]

太好了，你把改动拿回来了！
