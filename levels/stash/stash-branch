title = 从 stash 创建分支 branch
cards = checkout commit-auto merge reset-hard

[description]

如果你想保留这些改动，但它们不属于 main 分支 branch，可以很容易地从储藏的改动创建一个新分支。使用
    git stash branch <branchname> <stash>
如果只想使用最新的 stash 记录，可以省略 `<stash>` 参数。

从储藏的改动创建一个新分支 branch！

---
提示 1：先想好新分支 branch 的名字。
---
提示 2：最新的 stash 记录可以不写 `<stash>`。
---
提示 3：完成后仓库里应该至少有两个分支 branch。

[setup]

echo "Apple Pie:" > recipe

git add .
git commit -m "creating a recipe"

echo "- 4 Apples" >> recipe

git add .
git commit -m "Adding ingredients"

echo "- 500g Flour" >> recipe
git stash push

git checkout main

[win]

# 你从储藏的改动创建了新分支 branch 吗？
test "$(git branch --list| wc -l)" -ge 2

[actions]



[congrats]

储藏的改动已经进入新分支 branch！
