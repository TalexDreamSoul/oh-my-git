title = 从 stash 弹出改动
cards = checkout commit-auto merge reset-hard

[description]

当你已经储藏 stash 了改动，并想把它们应用回当前工作目录时，可以使用
    git stash pop
这会从 stash 栈中移除这份改动。如果你还想保留 stash 栈中的记录，可以使用
    git stash apply

---
提示 1：`git stash pop` 会应用改动，并移除对应 stash 记录。
---
提示 2：`git stash apply` 只应用改动，不移除记录。
---
提示 3：这个关卡需要把 stash 栈清空。

[setup]

echo "Apple Pie:" > recipe

git add .
git commit -m "creating a recipe"

echo "- 4 Apples" >> recipe

git add .
git commit -m "Adding ingredients"

echo "- 500g Flour" >> recipe

git stash push
git checkout main

[win]

# 你从 stash 栈中弹出了这些改动吗？
test "$(git stash list | wc -l)" -eq 0

[actions]



[congrats]

太好了，你把改动拿回来了！
