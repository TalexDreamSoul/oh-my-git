title = 在下一次提交 commit 中删除文件
cards = add reset-file checkout-file rm file-delete commit

[description]

如果想在下一次提交 commit 中删除一个文件，可以使用 `git rm`！它会同时删除本地文件，并把删除记录到暂存区 index。

如果文件已经被修改，需要先重置这些改动。

[setup]

echo a > a
echo x > b
echo x > c
git add .
git commit -m "Initial commit"
echo x > a
echo b > b
git add b

[win]

# 创建一个所有文件都被删除的提交 commit ¯\_(^_^)_/¯
test "$(git ls-tree main | wc -l)" -eq 0
