title = 从暂存区 index 检出文件
cards = add reset-file checkout-file commit

[description]

你修改了文件，但决定不保留这些改动！可以用 `git checkout` 来恢复。

如果你已经更新了暂存区 index，比如文件 c，那会怎样？这时必须先重置暂存区 index！

[setup]

echo a > a
echo b > b
echo c > c
git add .
git commit -m "Initial commit"
echo x > a
echo x > b
echo x > c
git add c

[win]

# 移除本地文件中的所有改动！
test "$(git diff --name-only | wc -l)" -eq 0
