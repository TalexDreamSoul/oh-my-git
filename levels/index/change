title = 更新暂存区 index 里的文件
cards = add commit

[description]

当我们修改文件时，暂存区 index 不会自己变化。必须使用 `git add`，把暂存区 index 更新为文件修改后的版本。

来试试看！

文件浏览器里的图标会告诉你：实际文件（白色）和暂存区 index 中的版本（蓝色）什么时候不同、什么时候相同！

[congrats]

很好！暂存区 index 有时也叫 “staging area”。当你使用 `git commit` 时，它里面的内容正是下一次提交 commit 会包含的内容！

[setup]

echo "The candle is burning with a blue flame." > candle
git add .
git commit -m "The beginning"

[win]

# 修改 candle。
test "$(git diff --name-only)" = "candle" || file -f .git/candle-changed && touch .git/candle-changed

# 添加 candle。
test "$(git diff --cached --name-only)" = "candle" || file -f .git/candle-added && touch .git/candle-added

# 创建一个提交 commit。
test "$(git diff --name-only HEAD HEAD^)" = "candle"
