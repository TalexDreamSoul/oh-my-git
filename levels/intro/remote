title = 一起协作
cards = pull commit-auto push

[description]

把你的名字加到学生名单里吧！

我的时间机器里已经有了这份名单的第二个提交 commit。我们来一起协作！

[cli]

想找回之前输入过的命令，可以按上/下方向键。这样就不用把长命令重新打一遍了。

[congrats]

欢迎来到时间旅行学校！明天第一堂课见！

[setup]

echo "~ List of current students ~" > students
git add .
git commit -m "Initial version"
git push -u teacher main

git update-ref -d refs/remotes/teacher/main

[setup teacher]

git reset --hard main

echo " 
- Sam
- Alex" >> students

git add .
git commit -m "Added two students"

[win]

# 用 `git pull` 从老师那里取得第二个提交 commit。
test "$(git log --oneline teacher/main | wc -l)" -ge 2

# 把你的名字加到学生名单里。
test "$(cat students |wc -l)" -ge 5

# 提交 commit 你的结果。
test "$(git show main:students |wc -l)" -ge 5

[win teacher]

# 然后用 `git push` 把结果发给老师！
test "$(git show main:students |wc -l)" -ge 5
