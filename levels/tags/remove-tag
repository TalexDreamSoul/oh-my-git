title = 删除标签 tag
cards = checkout commit-auto merge reset-hard

[description]

你添加了太多标签 tag？没问题！用下面的命令删除它们：

    git tag -d <tag-name>
    
删除这个仓库 repo 里的所有标签 tag！

---
提示 1：`git tag -l` 可以列出所有标签 tag。
---
提示 2：对每个标签使用 `git tag -d <tag-name>`。
---
提示 3：完成后标签列表应该为空。

[setup]

echo "event 1" > feature-list

git add .
git commit -m "Adding feature 1"

echo "event 2" >> feature-list

git add .
git commit -m "Adding feature 2"

echo "event 3" >> feature-list

git add .
git commit -m "Adding feature 3"

git tag v1 HEAD~2
git tag v2 HEAD~1
git tag v3 

git checkout --detach main

[win]

# 你删除了所有标签 tag 吗？
test "$(git tag -l | wc -l)" -eq 0

[actions]



[congrats]

做得好！
