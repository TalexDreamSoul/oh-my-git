title = 创建标签 tag
cards = checkout commit-auto merge reset-hard

[description]

有些提交 commit 可能很特别。也许你达成了一个里程碑，或者发布了一个新版本号。

你可以用一种叫作“标签 tag”的特殊标记来标出这些提交。

输入

    git tag <tag-name> 
    
即可给提交 commit 打标签。

---
提示 1：先 checkout 到你想打标签的提交 commit。
---
提示 2：标签 tag 名可以自己选择。
---
提示 3：`git tag -l` 可以查看已有标签。

[setup]

echo "event 1" > feature-list

git add .
git commit -m "Adding feature 1"

echo "event 2" >> feature-list

git add .
git commit -m "Adding feature 2"

echo "event 3" >> feature-list

git add .
git commit -m "Adding feature 3"

git checkout --detach main

[win]

# 你创建了新的标签 tag 吗？
test "$(git tag -l | wc -l)" -ge 1

[actions]



[congrats]

很好！你给第一个提交 commit 打上了标签 tag。
