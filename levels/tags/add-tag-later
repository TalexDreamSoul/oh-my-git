title = 之后再打标签 tag
cards = checkout commit-auto merge reset-hard

[description]

如果你忘了给当前提交 commit 打标签怎么办？
没问题！也可以给更早的提交打标签：

    git tag <tag-name> <commit-hash>
    
给提交信息为 "Adding feature 2" 的提交 commit 打上名为 `v1` 的标签 tag！

---
提示 1：先找到 "Adding feature 2" 对应的提交 commit。
---
提示 2：你可以在命令后面写提交 hash。
---
提示 3：目标标签名必须是 `v1`。

[setup]

echo "event 1" > feature-list

git add .
git commit -m "Adding feature 1"

echo "event 2" >> feature-list

git add .
git commit -m "Adding feature 2"

echo "event 3" >> feature-list

git add .
git commit -m "Adding feature 3"

git checkout --detach main

[win]

# 你创建了新的标签 tag 吗？
test "$(git show v1 -s --format=%h)" = "$(git show HEAD~1 -s --format=%h)"

[actions]



[congrats]

做得好！
